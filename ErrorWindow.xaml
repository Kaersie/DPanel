<Window x:Class="DPanel.ErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DPanel"
        mc:Ignorable="d"
        Title="出现错误" Height="392" Width="538.958"
                TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="#E9EAEC" FontFamily="HarmonyOS Sans SC"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto" Loaded="Window_Loaded">
    <Grid Margin="0,0,5.556,3.889">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DockPanel>
            <TextBox x:Name="ErrorLabel" HorizontalAlignment="Left" Margin="5" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" VerticalScrollBarVisibility="Visible" />
        </DockPanel>

    </Grid>
</Window>
